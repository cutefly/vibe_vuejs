spring:
  datasource:
    url: jdbc:mysql://dev-db:3306/vibe_db
    username: ${DB_USERNAME:vibe_user}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
